@model IEnumerable<CrossSell_App.DataAccess.Questioner>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Questioner")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayName("Metadata")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Questioner1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Metadata.Metadata_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Questioner_Id }) |
            
            @Html.ActionLink("Delete", "Delete", new { id=item.Questioner_Id })
        </td>
    </tr>
}

</table>
